{
  "name": "financial-house-code-test-node-server",
  "version": "1.0.0",
  "description": "Basic node server for financial house react project",
  "engines": {
    "node": "10.x"
  },
  "scripts": {
    "start": "node server",
    "build": "cd client/ && npm install && npm test && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "client/node_modules"
  ],
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.16.4",
    "request": "^2.88.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hamdiceylan/financial-house-test.git"
  },
  "license": "MIT",
  "devDependencies": {}
}
